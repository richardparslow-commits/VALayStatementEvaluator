# k8s-ingress.yaml — Ingress with cookie-based session affinity
#
# Requires an nginx-ingress controller installed in the cluster.
# The cookie-based affinity ensures each user consistently hits the same
# Streamlit backend, preserving their session across page reloads.
#
# KEEP these annotations in Pattern C too. Affinity is not a workaround: a
# Streamlit session is a live WebSocket bound to one server process, so a
# request that lands on a pod not holding the session gets "session not found".
# Pattern C removes the *heavy pipeline work* from the web pod (see
# k8s-worker.yaml), which is what made affinity a hot-spot problem — it does not
# remove the need for affinity.

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: va-lse
  namespace: va-lse
  annotations:
    # --- Session affinity: cookie-based (works behind NAT) ---
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "VA_LSE_SESSION"
    nginx.ingress.kubernetes.io/session-cookie-max-age: "3600"
    nginx.ingress.kubernetes.io/session-cookie-path: "/"
    # --- Streamlit WebSocket support ---
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    # --- Timeouts for long LLM calls ---
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    # --- TLS (uncomment and configure for production) ---
    # nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  # tls:
  #   - hosts:
  #       - va-lse.example.com
  #     secretName: va-lse-tls
  rules:
    - host: va-lse.example.com    # Replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: va-lse
                port:
                  number: 80
